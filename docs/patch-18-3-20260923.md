# 18.3 official announcement maintenance

Official source publication: `2026-09-22T18:00:00.000Z`, September 23 at 02:00 in Asia/Shanghai. Revision calendar date is `2026-09-23`; the source timestamp is retained separately.

- English: https://teamfighttactics.leagueoflegends.com/en-us/news/game-updates/teamfight-tactics-patch-18-3/
- Traditional Chinese: https://teamfighttactics.leagueoflegends.com/zh-tw/news/game-updates/teamfight-tactics-patch-18-3/

Append 74 reviewed numerical changes as `18.3-release-2026-09-23`. Preserve all three existing revisions and their 186 changes. Current season timeline has four linked nodes and 260 changes. Update the current announcement and season banner to 18.3. Website, generated mini-program data, `patch_facts`, composition-associated official evidence, and bounded semantic documents use the same revision data.

Editorial decisions:

- Separate Blossom-enhanced Wisps from ordinary Wisps; Combust therefore has distinct 22→18 and 18→15 changes in this patch.
- Preserve separate AD and AP forms for Gromp and Nidalee. Statikk Shiv refers to the Artifact, not the standard Void Staff identifier.
- Nidalee's official armor-penetration bullet conflicts with its adjacent bugfix and explicitly says “no functional change”. Exclude it from numeric buffs/nerfs and disclose the conflict in the revision summary. Do not infer an authoritative penetration change from these contradictory lines.
- Both Blossom's Call threshold rows are labeled four-cost in both official locales. Preserve both series with distinct IDs and make the duplicated official label explicit, without guessing that one means five-cost.
- Heated Rivalry's “Takedowns” count is recorded as mixed because the source does not establish whether it is a threshold or a reward limit. The loss of health in Sinister Deal decreases, so that individual component is a buff despite the overall Blossom nerf.
- Rules, augment availability, and bug fixes remain outside the established numerical-only list; the summary links to the full source. No fabricated before values for non-numerical updates.

Release is based on production `096e099ae77967a98aa99029dc09198dfd621c1d` in an isolated worktree. The root workspace contains unrelated development and is not a release candidate. No runtime routing, tool permissions, database migrations, or dependency changes are included. The existing official-news patch resolver continues to determine the runtime patch rather than forcing statistical data to a new patch.

Verification includes numerical source comparison, history/source/locale/entity tests, mini-program generation checks, canonical main and integration lanes, Agent evaluation, and real-provider smoke queries for current Warwick and Artifacts plus explicit 18.2 and 18.1 history. Deployment uses a backed-up app-only candidate with rollback; operational results are recorded separately after activation.

Live candidate testing exposed an existing Artifact-ranking false positive: `电刃（神器）：基础法强 15% → 25%` was rejected as a statistical ranking despite valid patch evidence. The colon-header branch now stops at parentheses; explicit ranking/recommendation words retain the existing scope check. Regression cases accept Chinese/ASCII parenthetical category labels, reject actual unsupported Artifact rankings, and still reject invented numerical facts. No evidence schema, numeric grounding, or statistical authority is relaxed.
