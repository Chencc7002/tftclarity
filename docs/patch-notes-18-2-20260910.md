# 18.2 更新公告维护

核实日期：2026-09-10。Riot 正式公告发布时间为 `2026-09-09T18:00:00.000Z`，即北京时间 9 月 10 日 02:00。

官方来源：

- [英文正式公告](https://teamfighttactics.leagueoflegends.com/en-us/news/game-updates/teamfight-tactics-patch-18-2/)
- [繁体中文正式公告](https://teamfighttactics.leagueoflegends.com/zh-tw/news/game-updates/teamfight-tactics-patch-18-2/)

网页端新增 18.2 中英文公告，包含 14 组、157 项数值对照。现有 18.1 历史数据保持原样。`season-context.js` 的公告入口、副标题和顶部提示同步切换至 18.2。顶部提示为：

> S18“魔法森林”18.2 版本已上线，更新公告已同步；本站 S17 数据查询暂时停用。

小程序内容由网页公告生成，更新后运行 `node scripts/sync-miniprogram-patch-notes.mjs`，使用 `--check` 验证一致性。

编辑规则与原文异常：

- 延续现有数值公告范围，功能、外观、问题修复、新增奖励组合和规则改动不纳入数值链。魔女奖励中的纯金币变动保留；替换装备或弈子的组合不单独抽出金币标为削弱。
- 阿狸、凯尔、绯红印记树怪、塔里克与 Combust 的增强和削弱分项标记。夜之锋刃及其光明版本的触发机制调整用琥珀色“调整”，避免仅凭血量门槛下降判为削弱。
- 维迦的 1.5% → 3% 是说明文字修复，官方明确表示实际效果未变，不作为数值增强收录。
- 乐芙兰范围伤害旧值列出四档、新值只有三档；仅收录双方明确列出的前三档，不推测四星新值。
- 智慧末刃原文列出五档伤害但标注 stage 2–5；保留五档伤害，不推测阶段对应。
- Hold The Line 新值缺少百分号，按同句所指 AD/AP 加成保留百分比格式。
- 英雄和羁绊中文名优先采用仓库现有显示字典。灵火译名附英文名称，尚未核实的强化符文中文名使用英文。

本次仅维护公告展示与顶部提示；数据源的 `source.currentPatch` 回退值和 Agent 官方 Evidence 库不由公告内容推导或覆盖，实际查询继续使用现有 `latest/current` 解析路径。

验证结果（使用 bundled Node.js 24.19.0）：

- 定向测试 92/92 通过，覆盖公告可达性、中英文标识、小程序逐项一致性、混合调整分类与旧版历史兼容。
- 小程序生成检查 `node scripts/sync-miniprogram-patch-notes.mjs --check` 通过。
- 主回归 1492 通过、7 跳过、1 失败：`test/release-config.test.js:140` 要求 README 包含“发布就绪状态”链接，当前工作区 README 缺少该链接。README 和该测试均在本次任务开始前已有修改，本次未修改这两个文件。
- 集成回归 235 通过、1 跳过、0 失败。
- Agent 离线评测 50/50 通过。
- 本地浏览器打开真实工具入口，确认中英文公告标题、157 项变更、前后数值及三色图例正常显示；顶部提示和副标题均显示 18.2。此前的浏览器额度拦截已在用户要求继续后恢复，视觉检查已补齐。

本记录对应工作区修改和本地验证，尚未部署到生产站点。

## 独立发布候选

以线上提交 `bb00a67baac32dc2817b6d39158ffb9d68ab0700` 为基础，仅合入公告维护。保留生产公告既有实体元数据和缺省版本回退，以及线上语言策略。小程序补齐数值历史渲染。独立发布树主回归 1558 通过、7 跳过、0 失败；定向测试 91/91，离线评测 50/50。

旧 readiness 文档的 2026-08-11 首发阻塞记录已过时；本次实际线上基线为上述 SHA，存在正常运行的 HTTPS 入口及 PostgreSQL/Redis。最终镜像验收和部署结果追加至本次生产发布记录。
