# 转职排行与自由聊天发布验收

从生产提交 `778764ef1aa236c9a75f2956f217376c45f7df49` 建立独立发布分支，仅移入转职排行榜和 Agent 调用增量，保留现有数值补丁工具、语言策略、Skills control 与其他生产功能。主工作区仍包含其他开发，不作为发布快照。

发布树验证：功能及 Agent 架构相关测试 257/257；main lane 1518 通过、7 条件跳过；integration lane 235 通过、1 条件跳过；Agent 评估 50/50。使用 Node 24.19.0。依赖和数据库结构无变更。

发布流程：备份并校验 PostgreSQL dump、保留旧镜像；构建最终发布镜像；在候选镜像内使用生产配置执行真实排行、比较和携带者查询后，仅替换 app，验证 readiness、生产配置及公网结果。

仅关闭聊天入口可设置 `TFT_AGENT_REACT_EMBLEM_RANKINGS=off` 并重建 app 容器；快捷查询保持可用。完整回滚使用服务器 `backups/emblem-rankings-20260908` 中记录的旧提交与镜像。本次无数据库迁移，不需要数据库回退。

较早的 R1 readiness 状态早于已完成的生产发布。本次以当前生产基线、最终发布镜像与本次变更验收为准，不重新宣称历史全部产品验收通过。实际部署结果与最终 SHA/image 在部署完成后另行记录。
